cmake_minimum_required(VERSION 3.20)

# Tell CMake that EditorDebug is a Debug-type config, EditorRelease is Release-type
set(CMAKE_CONFIGURATION_TYPES "Debug;Release;EditorDebug;EditorRelease" CACHE STRING "" FORCE)
set_property(GLOBAL PROPERTY DEBUG_CONFIGURATIONS "Debug;EditorDebug")

# Map custom configurations to standard ones for vcpkg imported targets
# Without this, vcpkg doesn't know EditorRelease should use Release libraries
set(CMAKE_MAP_IMPORTED_CONFIG_EDITORRELEASE Release)
set(CMAKE_MAP_IMPORTED_CONFIG_EDITORDEBUG Debug)

# vcpkg integration - for Linux and Windows builds
# Only Android uses prebuilt libraries
if(NOT CMAKE_SYSTEM_NAME STREQUAL "Android")
    # Put vcpkg_installed in Project/Build/ instead of Build/Intermediate/
    set(VCPKG_INSTALLED_DIR "${CMAKE_CURRENT_SOURCE_DIR}/Build/vcpkg_installed"
        CACHE PATH "Vcpkg installed directory")
    set(CMAKE_TOOLCHAIN_FILE "${CMAKE_CURRENT_SOURCE_DIR}/../vcpkg/scripts/buildsystems/vcpkg.cmake"
        CACHE STRING "Vcpkg toolchain file")
endif()

# Project definition
project(GAM300 VERSION 1.0.0 LANGUAGES C CXX)

# MSVC Runtime Library - automatic Debug/Release selection
# DEBUG_CONFIGURATIONS property tells CMake which configs are "debug"
set(CMAKE_MSVC_RUNTIME_LIBRARY "MultiThreaded$<$<OR:$<CONFIG:Debug>,$<CONFIG:EditorDebug>>:Debug>DLL")

# C++ Standard
set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)

# C Standard (for C files like glad.c and Lua sources)
set(CMAKE_C_STANDARD 99)
set(CMAKE_C_STANDARD_REQUIRED ON)

# Import dependencies using centralized module
include(ImportDependencies.cmake)
importDependencies()

# Build configurations
if(NOT CMAKE_BUILD_TYPE)
    set(CMAKE_BUILD_TYPE Release)
endif()

# MSVC warning suppressions
if(MSVC)
    add_compile_options(
        /Zm500
        /bigobj
        # C4251: 'class X needs dll-interface to be used by clients of class Y'
        # Source: Engine headers with exported classes containing STL members
        # Reason: Safe when all code is built together with same compiler/runtime.
        #         Only a real issue when distributing DLLs to third parties.
        /wd4251

        # C4275: 'non dll-interface class X used as base for dll-interface class Y'
        # Source: Engine headers with exported classes containing STL members
        # Reason: Safe when all code is built together with same compiler/runtime.
        #         Only a real issue when distributing DLLs to third parties.
        /wd4275

        # Source: Warnings from third-party libraries (Lua, Jolt, RapidJSON, ImGuizmo, FileWatch, assimp, xscheduler).
        # Reason: Ignore third-party library warnings.
        /wd4068   # C4068: 'unknown pragma'
        /wd4100   # C4100: 'identifier': unreferenced formal parameter (xscheduler headers)
        /wd4244   # C4244: 'conversion from X to Y, possible loss of data'
        /wd4245   # C4245: 'conversion from int to unsigned, signed/unsigned mismatch'
        /wd4267   # C4267: 'conversion from size_t to int, possible loss of data'
        /wd4310   # C4310: 'cast truncates constant value'
        /wd4324   # C4324: 'structure was padded due to alignment specifier'
        /wd4334   # C4334: 'result of 32-bit shift implicitly converted to 64 bits'
        /wd4701   # C4701: 'potentially uninitialized local variable'
        /wd4702   # C4702: 'unreachable code'
        /wd6255   # C6255: '_alloca indicates failure by raising a stack overflow exception'
        /wd6385   # C6385: 'reading invalid data from buffer'
        /wd6386   # C6386: 'buffer overrun while writing'
        /wd26439  # C26439: 'this kind of function should not throw, declare it noexcept'
        /wd26495  # C26495: 'variable is uninitialized, always initialize a member variable'
        /wd26827  # C26827: 'did you forget to initialize an enum constant'
        /wd33010  # C33010: 'unchecked lower bound for enum used as index'

        # Disable MSVC Code Analysis (C26xxx, C6xxx, C28xxx, C33xxx warnings)
        # Source: Third-party libraries (Jolt, rapidjson) and false positives
        # Reason: These are static analysis warnings, not compiler warnings.
        /analyze-

        # Exclude vcpkg external headers from static analysis
        # This tells MSVC to treat these paths as "external" and not analyze them
        /external:I "${VCPKG_INSTALLED_DIR}/x64-windows/include"
        /external:W0
    )
endif()

# Platform-specific compiler flags
if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -Wextra")
    set(CMAKE_CXX_FLAGS_DEBUG "${CMAKE_CXX_FLAGS_DEBUG} -g -O0")
    set(CMAKE_CXX_FLAGS_RELEASE "${CMAKE_CXX_FLAGS_RELEASE} -O3 -DNDEBUG")
    set(CMAKE_CXX_FLAGS_RELWITHDEBINFO "${CMAKE_CXX_FLAGS_RELWITHDEBINFO} -O3 -DNDEBUG")
    set(CMAKE_CXX_FLAGS_EDITORDEBUG "${CMAKE_CXX_FLAGS_DEBUG}")
    set(CMAKE_CXX_FLAGS_EDITORRELEASE "${CMAKE_CXX_FLAGS_RELEASE}")
    set(CMAKE_C_FLAGS_EDITORDEBUG "${CMAKE_C_FLAGS_DEBUG}")
    set(CMAKE_C_FLAGS_EDITORRELEASE "${CMAKE_C_FLAGS_RELEASE}")
elseif(MSVC)
    # MSVC-specific flags
    # NOTE: Do NOT add /MD or /MDd here - CMAKE_MSVC_RUNTIME_LIBRARY handles it
    # Adding explicit runtime flags will override and conflict with that setting
    set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} /W4 /EHsc")
    set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} /W4")

    # Debug configs: /Zi (debug info), /Od (no optimization), /RTC1 (runtime checks)
    set(CMAKE_CXX_FLAGS_DEBUG "/Zi /Od /RTC1")
    set(CMAKE_CXX_FLAGS_EDITORDEBUG "/Zi /Od /RTC1")
    set(CMAKE_C_FLAGS_DEBUG "/Zi /Od /RTC1")
    set(CMAKE_C_FLAGS_EDITORDEBUG "/Zi /Od /RTC1")

    # Release configs: /O2 (optimize), /DNDEBUG
    set(CMAKE_CXX_FLAGS_RELEASE "/O2 /DNDEBUG")
    set(CMAKE_CXX_FLAGS_EDITORRELEASE "/O2 /DNDEBUG")
    set(CMAKE_C_FLAGS_RELEASE "/O2 /DNDEBUG")
    set(CMAKE_C_FLAGS_EDITORRELEASE "/O2 /DNDEBUG")

    # Linker flags for debug symbol generation (PDB files)
    # /DEBUG:FULL generates complete PDB files for debugging
    set(CMAKE_EXE_LINKER_FLAGS_DEBUG "/DEBUG:FULL" CACHE STRING "" FORCE)
    set(CMAKE_EXE_LINKER_FLAGS_EDITORDEBUG "/DEBUG:FULL" CACHE STRING "" FORCE)
    set(CMAKE_SHARED_LINKER_FLAGS_DEBUG "/DEBUG:FULL" CACHE STRING "" FORCE)
    set(CMAKE_SHARED_LINKER_FLAGS_EDITORDEBUG "/DEBUG:FULL" CACHE STRING "" FORCE)
endif()

# Output directories - separate clean output from intermediate files
# OUTPUT_DIR is set by presets, defaults to CMAKE_BINARY_DIR if not set
if(NOT OUTPUT_DIR)
    set(OUTPUT_DIR ${CMAKE_BINARY_DIR})
endif()
set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${OUTPUT_DIR})
set(CMAKE_LIBRARY_OUTPUT_DIRECTORY ${OUTPUT_DIR})
set(CMAKE_ARCHIVE_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR})  # .lib files stay in intermediate

# Download Tracy profiler GUI (Windows only, non-Release builds)
if(WIN32)
    set(TRACY_PROFILER_DIR "${CMAKE_CURRENT_SOURCE_DIR}/Libraries/tracy")
    set(TRACY_PROFILER_EXE "${TRACY_PROFILER_DIR}/tracy-profiler.exe")
    if(NOT EXISTS "${TRACY_PROFILER_EXE}")
        set(TRACY_VERSION "0.11.1")
        set(TRACY_ZIP "${TRACY_PROFILER_DIR}/tracy-profiler.zip")
        set(TRACY_URL "https://github.com/wolfpld/tracy/releases/download/v${TRACY_VERSION}/windows-${TRACY_VERSION}.zip")
        file(MAKE_DIRECTORY "${TRACY_PROFILER_DIR}")
        message(STATUS "Downloading Tracy profiler GUI v${TRACY_VERSION}...")
        file(DOWNLOAD "${TRACY_URL}" "${TRACY_ZIP}"
            STATUS TRACY_DL_STATUS
            TIMEOUT 60
        )
        list(GET TRACY_DL_STATUS 0 TRACY_DL_CODE)
        if(TRACY_DL_CODE EQUAL 0)
            message(STATUS "Extracting tracy-profiler.exe only...")
            file(ARCHIVE_EXTRACT INPUT "${TRACY_ZIP}" DESTINATION "${TRACY_PROFILER_DIR}"
                PATTERNS "tracy-profiler.exe")
            file(REMOVE "${TRACY_ZIP}")
            if(EXISTS "${TRACY_PROFILER_EXE}")
                message(STATUS "Tracy profiler GUI ready at: ${TRACY_PROFILER_EXE}")
            else()
                message(WARNING "Tracy profiler GUI download succeeded but exe not found at expected path")
            endif()
        else()
            message(WARNING "Failed to download Tracy profiler GUI (code: ${TRACY_DL_CODE}). You can download manually from: ${TRACY_URL}")
            file(REMOVE "${TRACY_ZIP}")
        endif()
    endif()
endif()

# Platform-specific dependencies
if(ANDROID)
    # Android uses OpenGL ES, not desktop OpenGL
    find_library(GLES3_LIBRARY GLESv3)
    find_library(EGL_LIBRARY EGL)
    set(OPENGL_LIBRARIES ${GLES3_LIBRARY} ${EGL_LIBRARY})
else()
    # Desktop platforms (Linux/Windows) use OpenGL
    find_package(OpenGL REQUIRED)
    if(CMAKE_SYSTEM_NAME STREQUAL "Linux")
        find_package(PkgConfig REQUIRED)
    endif()
endif()

if (WIN32)
    set(APP_ICON_RESOURCE "${CMAKE_SOURCE_DIR}/app_icon.rc")
endif()

# Add Lua
add_subdirectory("${CMAKE_SOURCE_DIR}/Libraries/lua")

# Always build Engine (always shared library)
add_subdirectory(Engine)

# Always build Game (executable or static lib depending on config)
add_subdirectory(Game)

# Host tool: compiles source art into the .dds/.mesh the game loads. Built for
# desktop Game configurations only; it guards itself for Editor and Android.
add_subdirectory(Tools/AssetCooker)

# Only add Editor for Editor configurations
# (Single-config generators like Ninja know CMAKE_BUILD_TYPE at configure time)
if(CMAKE_BUILD_TYPE MATCHES "Editor")
    add_subdirectory(Editor)
endif()

# ==============================================================================
# CPack — Installer (Windows only, requires NSIS to be installed)
# Place required files before building installer:
#   Project/Installer/DigiPen_EULA.txt
#   Project/Installer/VC_redist.x64.exe
# ==============================================================================
if(WIN32 AND NOT ANDROID)
    set(CPACK_GENERATOR "NSIS")

    set(CPACK_PACKAGE_NAME                "Kusane")
    set(CPACK_PACKAGE_VERSION             "0.1")
    set(CPACK_PACKAGE_VENDOR              "DigiPen Institute of Technology")
    set(CPACK_PACKAGE_DESCRIPTION_SUMMARY "Kusane")
    set(CPACK_PACKAGE_FILE_NAME           "Kusane_Setup")
    set(CPACK_PACKAGE_INSTALL_DIRECTORY   "DigiPen/Kusane")

    # EULA (required for DigiPen TCR)
    if(EXISTS "${CMAKE_SOURCE_DIR}/Installer/DigiPen_EULA.txt")
        set(CPACK_RESOURCE_FILE_LICENSE "${CMAKE_SOURCE_DIR}/Installer/DigiPen_EULA.txt")
    endif()

    # Icons
    set(CPACK_NSIS_MUI_ICON    "${CMAKE_SOURCE_DIR}/Resources/SetupIcon.ico")
    set(CPACK_NSIS_MUI_UNIICON "${CMAKE_SOURCE_DIR}/Resources/SetupIcon.ico")

    # Display names
    set(CPACK_NSIS_DISPLAY_NAME "Kusane")
    set(CPACK_NSIS_PACKAGE_NAME "Kusane")

    # Start menu shortcut + desktop shortcut
    set(CPACK_PACKAGE_EXECUTABLES  "Kusane" "Kusane")
    set(CPACK_CREATE_DESKTOP_LINKS "Kusane")

    # No admin required (DigiPen TCR — sandbox machines)
    # Installs to %LOCALAPPDATA%\DigiPen\Kusane (always writable)
    set(CPACK_NSIS_DEFINES      "RequestExecutionLevel user")
    set(CPACK_NSIS_INSTALL_ROOT "$LOCALAPPDATA")

    # Run VC redist silently then remove from install dir
    set(CPACK_NSIS_EXTRA_INSTALL_COMMANDS
        "ExecWait '\\\"$INSTDIR\\\\VC_redist.x64.exe\\\" /q'\nDelete '$INSTDIR\\\\VC_redist.x64.exe'"
    )

    # "Run Kusane" checkbox on finish page
    set(CPACK_NSIS_MUI_FINISHPAGE_RUN "Kusane.exe")

    include(CPack)
endif()